class Solution {
public:
    int mySqrt(int x) {
            // int num = 8;
    int root = sqrt(x);  // calculates square root
    // cout << "Square root of " << num << " is " << root << endl;
    return root;

    }
};